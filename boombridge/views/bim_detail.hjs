<!DOCTYPE html>
<html>
  <head>
    <title>BIM Object Detail</title>
    <link rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  </head>

  <body class="bg-light p-4">

    <div class="container">

      <a href="/bim" class="btn btn-outline-secondary mb-3">← Back to BIM List</a>

      <div class="row">
        <div class="col-md-5">
          <!-- Thumbnail BIM -->
          <img src="/bim_thumbs/{{item.bim_thumbnail}}"
                onerror="this.src='/images/no-image.png'"
                class="img-fluid rounded shadow-sm mb-3">
        </div>

        <div class="col-md-7">
          <h2>{{item.bim_name}}</h2>
          <p class="text-muted">{{item.vmd_sncs}}</p>

          <!-- Tabs -->
          <ul class="nav nav-tabs" id="bimTabs">
            <li class="nav-item">
              <a class="nav-link active" data-bs-toggle="tab" href="#overview">Overview</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" data-bs-toggle="tab" href="#spec">Technical Specs</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" data-bs-toggle="tab" href="#download">Download</a>
            </li>
          </ul>

          <div class="tab-content p-3 border border-top-0 rounded-bottom">

            <!-- Overview TAB -->
            <div id="overview" class="tab-pane fade show active">
              <h5>Product Information</h5>
              <p><strong>BIM Code:</strong> {{item.bim_code}}</p>
              <p><strong>Product ID:</strong> {{item.product_id}}</p>
            </div>

            <!-- Specification TAB -->
            <div id="spec" class="tab-pane fade">
              <h5>Specifications</h5>
              <pre style="white-space: pre-wrap;">{{item.bim_notes}}</pre>
            </div>

            <!-- Download TAB -->
            <div id="download" class="tab-pane fade">
              <h5>Download BIM File</h5>

              {{#item.bim_file}}
              <a class="btn btn-success"
                 href="/bim/{{item.product_id}}/download">
                 Download {{item.bim_file}}
              </a>
              {{/item.bim_file}}

              {{^item.bim_file}}
              <p class="text-danger">No BIM file available.</p>
              {{/item.bim_file}}
            </div>

          </div>

        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
